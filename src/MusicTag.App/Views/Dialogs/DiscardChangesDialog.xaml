<ui:FluentWindow x:Class="MusicTag.App.Views.Dialogs.DiscardChangesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Discard Changes?" Height="220" Width="440"
        MinHeight="200" MinWidth="380"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Acrylic"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Discard Changes?" />

        <TextBlock Grid.Row="1" Margin="16" TextWrapping="Wrap"
                   Text="Some files have unsaved changes. Continuing will discard those changes and clear the undo history for the currently open folder. Continue?" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16">
            <ui:Button Content="Cancel" Margin="0,0,8,0" Click="OnCancelClick" />
            <ui:Button Content="Discard Changes" Appearance="Danger" Click="OnDiscardClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
