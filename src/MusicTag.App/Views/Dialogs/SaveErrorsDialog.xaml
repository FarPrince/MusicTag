<ui:FluentWindow x:Class="MusicTag.App.Views.Dialogs.SaveErrorsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Save Errors" Height="360" Width="480"
        MinHeight="240" MinWidth="360"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Acrylic"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Save Errors" />

        <TextBlock Grid.Row="1" Margin="16,8"
                   TextWrapping="Wrap"
                   Text="The following files could not be saved. Other files in the batch were saved successfully." />

        <ListBox Grid.Row="2" Margin="16,0,16,8"
                 ItemsSource="{Binding Failures}"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,4">
                        <TextBlock Text="{Binding FileName}" FontWeight="Bold" />
                        <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Opacity="0.8" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <ui:Button Grid.Row="3" Content="Close" HorizontalAlignment="Right"
                   Margin="16" Click="OnCloseClick" />
    </Grid>
</ui:FluentWindow>
